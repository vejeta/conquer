#!/bin/bash

set -e

case "$1" in
    remove|upgrade|deconfigure)
        # Clean up any temporary files if needed
        echo "Removing conquer package..."
        ;;

    failed-upgrade)
        ;;

    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

#DEBHELPER#

exit 0
